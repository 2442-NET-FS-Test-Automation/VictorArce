namespace Library.Data.Entities;

public enum Priority
{
    //By default enus are backed by ordinals (0,1,2, etc.) 
    // but you can also explicitly set the value of each enum member.
    normal = 0,
    Exédited = 1
}